/* Master-detail layout */
UserPage {
    layout: horizontal;
    height: 1fr;
}

UserListSection {
    width: 40%;
    height: 100%;
    padding: 1;
}

UserListSection DataTable {
    height: 1fr;
}

UserDetailSection {
    width: 60%;
    height: 100%;
    padding: 1;
    overflow-y: auto;
}

/* Hidden utility */
.hidden {
    display: none;
}

/* AddUserBar */
AddUserBar {
    height: auto;
    layout: horizontal;
    padding: 0 0 1 0;
}

AddUserBar Input {
    width: 1fr;
}

AddUserBar Button {
    margin-left: 1;
}

/* Detail placeholder */
#detail-placeholder {
    width: 100%;
    height: 100%;
    content-align: center middle;
    color: $text-muted;
}

#detail-content {
    height: auto;
}

#password-input {
    margin-top: 1;
}

/* Fetch bar */
#fetch-bar,
#submit-form {
    height: auto;
    align: right middle;
    layout: horizontal;
}

#fetch-bar Input {
    width: 1fr;
}

#fetch-bar Button {
    margin-left: 1;
    margin-top: 1;
}

#fetch-button {
    width: auto;

}

/* Fetch releases bar */
#fetch-release-bar {
    height: auto;
    layout: horizontal;
    margin-top: 1;
}

#artist-select {
    width: 1fr;
}

#fetch-release-button {
    width: auto;
    margin-left: 1;
}

FetchReleases {
    width: auto;
    height: 8;
    border: solid grey;
    margin-top: 1;
}

/* FetchArtists progress */
FetchArtists {
    width: auto;
    height: 8;
    border: solid grey;
}

#fetch-counter,
#fetch-release-counter {
    text-align: right;
    width: 100%;
}

#fetch-log,
#fetch-release-log {
    overflow-x: hidden;
    max-height: 4;
    height: 4;
}

/* Settings */
.setting-label {
    text-style: bold;
}

#min-scrobbles-input,
#releases-not-before-input {
    width: 100%;
}
#submit-form {
    align: right middle;
}
#submit-form Button {
    margin-top: 1;
}


/* Confirm-delete modal */
ConfirmDeleteScreen {
    align: center middle;
}

#confirm-dialog {
    width: 40;
    height: auto;
    padding: 1 2;
    border: thick $accent;
    background: $surface;
}

#confirm-question {
    width: 100%;
    text-align: center;
    margin-bottom: 1;
}

#confirm-buttons {
    width: 100%;
    height: auto;
    align-horizontal: center;
}

#confirm-buttons Button {
    margin: 0 1;
}
